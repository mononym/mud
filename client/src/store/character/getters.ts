import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { CharacterInterface } from './state';

const getters: GetterTree<CharacterInterface, StateInterface> = {
};

export default getters;

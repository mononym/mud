import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { AreaInterface } from './state';

const getters: GetterTree<AreaInterface, StateInterface> = {
};

export default getters;

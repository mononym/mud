import { GetterTree } from 'vuex';
import { StateInterface } from '../index';
import { MapInterface } from './state';

const getters: GetterTree<MapInterface, StateInterface> = {
};

export default getters;
